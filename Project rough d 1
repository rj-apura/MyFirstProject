import java.util.Scanner;
import java.io.*;

class Student {
    String name;
    int assignments;
    double[] grades; // = new int[courses];


    Student(String n, int a) {
        name = n;
        assignments = a;
        grades = new double[assignments];
    }

    // TODO: Make method
    double getAverage(){

    }

    double getLowest(){

    }

    double getHighest(){

    }

    void printInfo() {
        System.out.println("---" + name + "---");
        System.out.println("Grades");
        System.out.println("----------------");
        for (int i = 0; i < assignments; i++) {
            System.out.println(grades[i]);
        }
    }
}

public class Main {
    public static void main(String[] args) throws IOException {
            Scanner sc = new Scanner(System.in);

            // TODO: Check for File "students.txt"
            try {
                File dataFile = new File("students.txt");
                Scanner input = new Scanner(dataFile);
            } catch(Exception e){
                System.out.println();
            }

            // Grab Student Classes amount
            System.out.print("How many assignments? ");
            int assignmentsHolder = sc.nextInt();
            sc.nextLine();

            System.out.print("What is your name? ");
            String nameHolder = sc.nextLine();

            Student s = new Student(nameHolder, assignmentsHolder);

            double sum = 0.0;

            for (int i = 0; i < assignmentsHolder; i++) {
                System.out.print("Enter score for assignment "+(i+1)+": ");
                s.grades[i] = sc.nextDouble();
                sum+= s.grades[i];
            }

            s.printInfo();
            System.out.println("Grade Average: " + (sum/s.grades.length));
            // try catch
            // File studentData = new File("students.txt");

            // FileWriter writer = new FileWriter("students.txt");
            // writer.write("\n" + s.name + "\n");
            // writer.write("Courses: " + s.assignments + "\n");
            // writer.write("Grades: ");
            // for (int i = 0; i == s.assignments; i++) {
            //    writer.write(" "+s.grades[i]+" ");
            //}
    }
}
